# Angular latest (docker image)
To install and run the application use the command `docker compose up`

### Rebuilding
When dependencies are out of date use the command `docker compose build` to reinstall the application.